import React, { useContext } from "react";

const UserContext = React.createContext();

export default UserContext;

{/* <useContext>
    <login/>
    <card>               //provider example
       <data/>
    <card/>
<useContext/> */}

//context is just like the global varible and nothing more than it.
//user context me ham provider bhi provide karte hai. Provider ke help se ham data provide karate hai.
